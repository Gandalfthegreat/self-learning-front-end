1. 10,因为var i，i存在于全局作用域，console.log(i)调用的i指向同一块空间，因此打印时i为10
2.temporal dead zone, 虽然var声明了变量，但在使用let命令声明前，变量都不可用
4. var声明后作用域为全局，声明前调用，值为undefined
   let声明后只能在局部作用域使用， 声明前不能调用
   const声明后只能在局部作用域使用，声明后不可更改
5.setTimeout 内部，this指向执行上下文，由于obj调用了fn，所以执行上下文为obj；如果是普通函数则指向window
6. 在对象中，用于生成不可更改的key值，可用于私有变量使用
7. 浅拷贝后，新对象指向老对象的内存地址；深拷贝后，新老对象不共用同一个内存地址
8. Ts有类型检查，Ts基于Js
9. 优点： 类型检查完善，不易出错
   缺点： 编程效率降低，而且需要配置config
10. 原理，从根对象，比如dom出发，标记对象A中是否存在对象B，若有，则表明存在引用。最后对引用为0的对象清除
    优点： 简便，额外空间利用少
    缺点： 不能发现循环引用
11. a. 标记所有对象
    b. 将存活的对象移动到一整块内存，需要回收的内存直接清除，这样没有空间碎片

12. a. 首先在FROM区域存储对象
    b. FROM满后将对象拷贝到TO区域，以此类推
    c. 当对象来回拷贝两次后，移动到老生代

13. js主线程运行时，定时增量标记；
    把程序分片，轮流执行js主线程和垃圾回收任务